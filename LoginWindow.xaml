<!-- LoginWindow.xaml -->
<Window x:Class="TempusNexum.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Username:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="1" Margin="0,0,0,10"/>
        <TextBlock Text="Password:" Grid.Row="2" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,0,0,10"/>
        <Button Content="Login" Grid.Row="4" Width="100" Click="LoginButton_Click"/>
    </Grid>
</Window>